{% extends 'base.html.twig' %}

{% block title %}Utilisateur - {{user.id}}{% endblock %}

{% block body %}
    <div class="d-flex justify-content-center align-items-center gap-5 my-3">
        <a class="btn btn-secondary" href="{{ path('app_user_index') }}"><i class="fa-solid fa-arrow-left me-3"></i>Retour à la liste</a>
        <h1 class="text-center">Utilisateurs - {{user.id}}</h1>
    </div>

    {# <table class="table table-striped container">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ user.password }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ user.username }}</td>
            </tr>
            <tr>
                <th>Avatar</th>
                <td>{{ user.avatar }}</td>
            </tr>
            <tr>
                <th>IsSubscribed</th>
                <td>{{ user.isSubscribed ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>IsVerified</th>
                <td>{{ user.isVerified ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <div class="container">
        <a class="btn btn-success" href="{{ path('app_user_edit', {'id': user.id}) }}">Modifier</a>
        {{ include('user/_delete_form.html.twig') }}
    </div> #}

    {# <div class="card mx-auto" style="width: 20rem;">
        <img src="{{ user.avatar }}" class="card-img-top" alt="...">
        <div class="card-body">
            <div class="d-flex justify-content-between">
                <h5>{{ user.id }}</5>
                <h5 class="card-title">{{ user.username }}</h5>
            </div>
            <div class="d-flex justify-content-between mt-3">
                <h6 class="card-title">Mail:</h6>
                <p>{{ user.email }}</p>
            </div>
        </div>
        <ul class="list-group list-group-flush">
            <div class="d-flex justify-content-between px-2 mt-3">
                <h6 class="card-title me-5">Souscris à la newsletter :</h6>
                <p>{{ user.isSubscribed ? 'Oui' : 'Non' }}</p>
            </div>
            <div class="d-flex justify-content-between px-2 mt-3">
                <h6 class="card-title me-5">Email confirmé :</h6>
                <p>{{ user.isVerified ? 'Oui' : 'Non' }}</p>
            </div>
            <div class="d-flex justify-content-between px-2 mt-3">
                <h6 class="card-title me-5">Rôle :</h6>
                <p>{{ user.roles ? user.roles|json_encode : '' }}</p>
            </div>            
        </ul>
        <div class="card-body d-flex justify-content-center align-items-center gap-3">
            <a class="btn btn-success" href="{{ path('app_user_edit', {'id': user.id}) }}"><i class="fa-solid fa-pen me-2"></i>Modifier</a>
            {{ include('user/_delete_form.html.twig') }}
        </div>
    </div> #}

    <div class="d-flex justify-content-center align-items-center gap-5 mt-5 border border-2 mx-auto p-5" style="width: 50%">
        <div>
            <img style="height: 300px; width: 300px" src="{{ user.avatar }}" class="img-thumbnail rounded-circle" alt="...">
        </div>
        <div class="d-flex flex-column gap-3">
            <div class="d-flex justify-content-start">
                <h6>User ID :</h6>
                <h6 class="ms-3">{{ user.id }}</h6>
            </div>
            <div class="d-flex justify-content-start">
                <h6>Username :</h6>
                <h6 class="ms-3">{{ user.username }}</h6>
            </div>
            <div class="d-flex justify-content-start">
                <h6>Adresse mail :</h6>
                <h6 class="ms-3">{{ user.email }}</h6>
            </div>
            <div class="d-flex justify-content-start">
                <h6>Rôle de l'utilisateur :</h6>
                <h6 class="ms-3">{{ user.roles ? user.roles|json_encode : '' }}</h6>
            </div>
            <div class="d-flex justify-content-start">
                <h6>Souscris à la newsletter :</h6>
                <h6 class="ms-3">{{ user.isSubscribed ? 'Oui' : 'Non' }}</h6>
            </div>
            <div class="d-flex justify-content-start">
                <h6>Email confrimé :</h6>
                <h6 class="ms-3">{{ user.isVerified ? 'Oui' : 'Non' }}</h6>
            </div>
            <div class="card-body d-flex justify-content-center align-items-center gap-3 mt-4">
                <a class="btn btn-success" href="{{ path('app_user_edit', {'id': user.id}) }}"><i class="fa-solid fa-pen me-2"></i>Modifier</a>
                {{ include('user/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %}

