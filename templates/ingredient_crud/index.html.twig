{% extends 'base.html.twig' %}

{% block title %}Ingredient index{% endblock %}

{% block body %}
    <h1>Ingredient index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Type</th>
                <th>Description</th>
                <th>Image</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ingredient in ingredients %}
            <tr>
                <td>{{ ingredient.id }}</td>
                <td>{{ ingredient.type }}</td>
                <td>{{ ingredient.description }}</td>
                <td>{{ ingredient.image }}</td>
                <td>{{ ingredient.name }}</td>
                <td>
                    <a href="{{ path('app_ingredient_crud_show', {'id': ingredient.id}) }}">show</a>
                    <a href="{{ path('app_ingredient_crud_edit', {'id': ingredient.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_ingredient_crud_new') }}">Create new</a>
{% endblock %}
