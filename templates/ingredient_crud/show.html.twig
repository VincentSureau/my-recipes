{% extends 'base.html.twig' %}

{% block title %}Ingredient{% endblock %}

{% block body %}
    <h1>Ingredient</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ingredient.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ ingredient.type }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ ingredient.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ ingredient.image }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ingredient.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ingredient_crud_index') }}">back to list</a>

    <a href="{{ path('app_ingredient_crud_edit', {'id': ingredient.id}) }}">edit</a>

    {{ include('ingredient_crud/_delete_form.html.twig') }}
{% endblock %}
