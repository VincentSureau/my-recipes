{% set title = "Liste des newsletters" %}
{% extends 'admin_with_table.html.twig' %}

{% block actions %}
    <a class="btn btn-success" href="{{ path('app_admin_newsletter_new') }}">Ajouter</a>
{% endblock %}

    {% block table_content %}
        <thead>
            <tr>
                <th>Contenu de la newsletter</th>
                <th>Envoyé le</th>
                <th>Actions</a></th>
            </tr>
        </thead>
        <tbody>
        {% for newsletter in newsletters %}
            <tr>
                <td>{{ newsletter.content }}</td>
                <td>{{ newsletter.sendAt ? newsletter.sendAt|date('d/m/Y')}}</td>
                <td>
                    <a href="{{ path('app_admin_newsletter_edit', {'id': newsletter.id}) }}">Modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
{% endblock %}
